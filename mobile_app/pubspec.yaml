name: sela_mobile
description: A premium, native Arabic mobile application for Sela School & NGO Management Platform.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.0
  intl: ">=0.19.0 <0.21.0"
  google_fonts: ">=6.2.0 <9.0.0"
  url_launcher: ^6.3.2
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
